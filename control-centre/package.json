{
  "name": "hypetv-control-centre",
  "version": "1.5.4",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:local": "wrangler d1 migrations apply HYPETV_DB --local",
    "db:remote": "wrangler d1 migrations apply HYPETV_DB --remote"
  },
  "devDependencies": {
    "wrangler": "^4.30.0"
  }
}
